# Faculty Training Calendar Embed

This folder contains a standalone HTML page that embeds the hosted URI Faculty
Development Training Calendar in a responsive iframe.

Open `index.html` through a local or hosted web server to test the embed.
